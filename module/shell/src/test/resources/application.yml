# spring
spring:
  application:
    name: jkit-shell
  banner:
    location: classpath:banner/banner.txt
  main:
    banner-mode: off
  output:
    ansi:
      enabled: always
# logging
logging:
  config: classpath:logging/log4j2.yml
